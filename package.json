{
  "name": "superstore",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd back-end && npm run build  && cd ../front-end && npm --force run build && cd ../admin-panel && npm --force run build",
    "start": "node back-end/build/src/index.js",

    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
